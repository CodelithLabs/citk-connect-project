name: citk_connect
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  google_sign_in: ^6.2.1
  firebase_crashlytics: ^5.0.6 # Prepared for future integration

  # State Management
  flutter_riverpod: ^3.0.3
  hooks_riverpod: ^3.0.3
  flutter_hooks: ^0.21.3+1
  riverpod_annotation: ^3.0.3

  # Navigation
  go_router: ^17.0.1

  # UI & Animation
  cupertino_icons: ^1.0.8
  animations: ^2.0.11 # For professional motion system
  cloud_firestore: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^6.0.0
  riverpod_generator: ^3.0.3 # For Riverpod code generation
  build_runner: ^2.4.9 # Code generation tool
  go_router_builder: ^4.1.1

flutter:
  uses-material-design: true
